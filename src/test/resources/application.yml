spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/replai-postgres
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  mail:
    host: localhost
    port: 2525
    username: test
    password: test
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

app:
  domain: http://localhost:8080

ai:
  service:
    internal-key: test
