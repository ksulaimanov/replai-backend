spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/replai-postgres
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
app:
  domain: http://localhost:8080

ai:
  service:
    internal-key: test

MAIL_PASSWORD: test-brevo-key
MAIL_FROM: test@replai.local
